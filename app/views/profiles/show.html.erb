<div class="col-xs-12 col-md-8">
  <%= @profile.first_name %> <%= @profile.last_name %>
  <p>Membre depuis <%= @profile.created_at.strftime("%m/%Y") %></p>
  <p><%= link_to 'Editer mon profil', edit_profile_path(@profile), class: 'btn btn-rounded'%></p>
</div>
